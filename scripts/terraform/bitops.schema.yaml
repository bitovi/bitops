terraform:
  type: object
  properties:
    cli:
      type: object
      properties:
        target:
          type: string
          parameter: target
          required: false
    options:
      type: object
      properties:
        fetch-kubeconfig:
          type: boolean
          export_env: FETCH_KUBECONFIG
          default: true