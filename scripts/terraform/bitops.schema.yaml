terraform:
  type: object
  properties:
    options:
      type: object
      properties:
        fetch-kubeconfig:
          type: boolean
          export_env: FETCH_KUBECONFIG
          default: true