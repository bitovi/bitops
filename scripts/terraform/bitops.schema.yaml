terraform:
  type: object
  properties:
    options:
      type: object
      properties:
        cluster-name:
          type: string
          export_env: CLUSTER_NAME
        fetch-kubeconfig:
          type: boolean
          export_env: FETCH_KUBECONFIG
          default: true
        workspace:
          type: string
          export_env: TERRAFORM_WORKSPACE